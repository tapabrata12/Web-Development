// Components are nothing but function so to make functional components we first have to declare a function

function App() {
  return(
    // Everything written under that tag will show in the website and this is not HTML this is JavaScript XML.
   <h1>Hello: Everything written under that tag will show in the website and this is not HTML this is JavaScript XML. Later we can import our app component as a HTML tag link thing in JSX</h1>
  );
}
export default App;